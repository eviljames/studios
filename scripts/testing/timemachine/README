timemachine is an application that pre-records the last 10 seconds of audio for you
when you press record it will record the previous ten seconds up to realtime.

Requires JACK, GTK, libsndfile, LASH, liblo
